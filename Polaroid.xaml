<Window x:Class="postitSimulator.Polaroid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Polaroid"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Width="250"
        Height="300"
        ResizeMode="NoResize"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseRightButtonUp="Window_MouseRightButtonUp"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave">

    <Window.Resources>

        <!-- ContextMenu -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#2D2D30"/>
            <Setter Property="BorderBrush" Value="#3E3E42"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style TargetType="Button" x:Key="ColorButtonStyle">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="BorderBrush" Value="#555"/>
            <Setter Property="BorderThickness" Value="1"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    <ContextMenu x:Key="GlobalMenu">
        <MenuItem Header="🖼️ Trocar imagem" Click="SelectImage_Click"/>
        <MenuItem Header="✏ Trocar Detalhes" Click="WriteDetail_Click"/>
        <Separator/>
        <MenuItem Header="➕ Nova Polaroid" Click="AddPolaroid_Click"/>
        <MenuItem Header="➕ Novo Postit" Click="AddPostit_Click"/>
        <Separator/>
        <MenuItem Header="✕ Fechar" Click="CloseBtn_Click"/>
    </ContextMenu>

    </Window.Resources>

    <Window.RenderTransform>
        <ScaleTransform x:Name="WindowScale" ScaleX="1" ScaleY="1"/>
    </Window.RenderTransform>

    <Window.RenderTransformOrigin>
        <Point X="0.5" Y="0.5"/>
    </Window.RenderTransformOrigin>

    <Border x:Name="RootBorder"
        MouseLeftButtonDown="DragArea_MouseLeftButtonDown"
        MouseMove="DragArea_MouseMove"
        MouseLeftButtonUp="DragArea_MouseLeftButtonUp">
        <Grid>
            <!-- VIEW NORMAL -->
            <Border x:Name="NormalView"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="Transparent"
                    Padding="0"
                    CornerRadius="4">

                <Border.Effect>
                    <DropShadowEffect BlurRadius="15"
                                    ShadowDepth="3"
                                    Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Path Fill="White"
                        IsHitTestVisible="False"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Stretch="Fill"
                        Data="M0,0 L0,1 L0.89,1 L1,0.91 L1,0 Z"/>
                        <Path Fill="#cecece"
                        IsHitTestVisible="False"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Stretch="Fill"
                        Data="M0,0 L0,1 L0.89,1 L1,0.91 L0.89,0.91 L0.89,1 L0,1 L0,0 Z"/>
                    <Border Background="Transparent"
                            Padding="5">
                        <Viewbox Stretch="Uniform">
                            <Grid Width="240" Height="300">

                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="60"/>
                                </Grid.RowDefinitions>

                                <!-- IMAGEM -->
                                <Border Grid.Row="0"
                                        CornerRadius="2"
                                        ClipToBounds="True"
                                        Background="#F2F2F2">
                                    <Image x:Name="MyImage"
                                        Source="Assets/IMG.jpeg"
                                        Stretch="Uniform"/>
                                </Border>

                                <!-- TEXTO (parte branca da polaroid) -->
                                <TextBlock x:Name="DetailsText"
                                        Grid.Row="1"
                                        TextAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="#444"
                                        FontSize="12"/>
                            </Grid>
                        </Viewbox>
                    </Border>
                    
                    <Border 
                        Width="30"
                        Height="30"
                        Background="Transparent"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Cursor="SizeNWSE"
                        MouseLeftButtonDown="ResizeStart"
                        MouseMove="ResizeMove"
                        MouseLeftButtonUp="ResizeEnd"/>
                </Grid>
            </Border>

            <!-- VIEW DOCK (TRIÂNGULO) -->
            <Grid x:Name="DockView"
                Visibility="Collapsed"
                MouseLeftButtonDown="DragArea_MouseLeftButtonDown"
                MouseMove="DragArea_MouseMove"
                MouseLeftButtonUp="DragArea_MouseLeftButtonUp">

                <Path x:Name="DockedPath"
                    Fill="#F2F2F2"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center">
                </Path>
                <TextBlock x:Name="IconPost"
                    Text="🖼️"
                    Foreground="Black"
                    FontSize="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>
            </Grid>

        </Grid>
    </Border>
</Window>